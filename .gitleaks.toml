# Gitleaks configuration for dotfiles
# https://github.com/gitleaks/gitleaks

[extend]
# Use the default gitleaks config as base
useDefault = true

# Add custom allowlist rules here if needed for false positives
# Example:
# [[allowlist]]
# description = "Ignore example file"
# paths = ["path/to/file"]
